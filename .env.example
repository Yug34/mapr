# OpenAI API Key for text summarization (server-side only)
# Get your API key from: https://platform.openai.com/api-keys
# This should be set in Vercel dashboard, not in .env for production
# For local development with Vercel CLI, set this in .env.local
OPENAI_API_KEY=your_api_key_here
